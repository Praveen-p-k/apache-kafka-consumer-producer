export const SXT_QUERY_TEMPLATES = {
  SELECT_ALL_FROM: 'SELECT * FROM',
  DELETE_FROM: 'DELETE FROM',
  WHERE_ID_EQUALS: 'WHERE ID=',
};

export enum NodeEnvironments {
  LOCAL = 'local',
  DEV = 'dev',
  STAGE = 'stage',
  PROD = 'prod',
}
